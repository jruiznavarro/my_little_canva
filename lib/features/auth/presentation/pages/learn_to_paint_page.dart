import 'package:flutter/material.dart';

class LearnToPaintPage extends StatelessWidget {
  const LearnToPaintPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFFF5F5F5),
      child: const Center(
        child: Text('Aprende a Pintar'),
      ),
    );
  }
} 